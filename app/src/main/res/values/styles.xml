<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNavbar</item>
        <item name="colorPrimaryDark">@color/colorNavbarDark</item>
        <item name="colorAccent">@color/Highlight_grün</item>
        <item name="android:colorBackground">@color/Background</item>
        <item name="android:textColorSecondary">@color/Grüne_Striche</item>
        <item name="android:textColorPrimary">@color/Highlight_grün</item>

        <item name="colorControlNormal">@color/colorNavbar</item>
        <!--<item name="colorControlActivated">@color/colorPrimary</item>-->
        <!--<item name="colorControlHighlight">@color/colorPrimary</item>-->

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Splash screen Theme-->

    <style name="splashScreenTheme" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@xml/screensplash</item>
    </style>

</resources>
